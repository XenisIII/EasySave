<Window x:Class="EasySaveWPF.Views.CreateSaveView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Save" Height="300" Width="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Content="Create Save" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold" Grid.Row="0" />

        <StackPanel Orientation="Vertical" Grid.Row="1" VerticalAlignment="Center">
            <TextBox Name="txtName" Margin="5">Name</TextBox>
            <TextBox Name="txtSourcePath" Margin="5">Source Path</TextBox>
            <TextBox Name="txtDestinationPath" Margin="5">Destination Path</TextBox>
            <TextBox Name="txtType" Margin="5">Type</TextBox>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <Button Content="Cancel" Margin="5" Click="CancelButton_Click"/>
            <Button Content="Save et Quit" Margin="5" Click="SaveQuitButton_Click"/>
            <Button Content="Save et Create a New" Margin="5" Click="SaveCreateButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
